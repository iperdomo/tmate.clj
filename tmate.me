#!/usr/bin/env bash

set -eu

docker run \
       --rm \
       --name="$(date +%s)" \
       --interactive \
       --tty \
       --volume "${HOME}/.m2:/home/clojure/.m2" \
       --volume "$(pwd)":/project \
       iperdomo/tmate.clj
